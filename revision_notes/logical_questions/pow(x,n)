read the constraints carefully
i have converted the int n to long long n to include int_min case
and i ave halved every iteration, like if n==16, i calculate n=8 and just suare it,
good one
https://leetcode.com/problems/powx-n/submissions/1811923104/

class Solution {
public:
double help(double x,long long n)
{
  double ans=1;
        if(x==0)
        return 0;
        if(n==0)
        return 1.00000;
       if(n<0)
       return help(1/x,abs(n));
       int j=n/2;
       double a=help(x,j);
       ans=a*a;
       if(n%2)
       {
        return ans*=x;
       }
       return ans;

}
    double myPow(double x, int n) 
    {
      return help(x,n);
    }  
};
